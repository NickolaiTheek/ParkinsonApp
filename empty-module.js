// This module is intentionally left empty.
// It is used in metro.config.js to prevent certain Node.js-specific modules
// like 'ws' or 'stream' from being bundled in React Native, where they would cause errors. 